[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA-ish: everything is one page, but keep a fallback just in case.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
